# Pseudo Code Solution

```text
    Initialize an empty tables list
    for i = 1 to 12
        Initialize an empty entries List
        for j = 1 to 12
            Multiply i with j
            Push the result to the entries list
        Push the entries list to the tables list
    Return the tables list
```
